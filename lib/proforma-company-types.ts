/** Seller block on proforma invoice — loaded from ERP-Backend default invoice settings. */
export type ProformaCompanyBlock = {
  legalName: string
  addressLines: string[]
  panCinLine: string
  telEmailLine: string
  bankLines: string[]
}
