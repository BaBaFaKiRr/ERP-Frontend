/**
 * Public logo paths (`public/` filenames contain spaces — use encoded URLs).
 * Mark (transparent): use in UI and favicon. Full (black canvas): `LEJER 2D logo.png` in public.
 */
export const LEJER_LOGO_MARK_SRC = '/Leger%202D%20logo%20bg-removed.png'
